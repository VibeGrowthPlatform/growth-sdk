# Basic Integration

Attach `VibeGrowthInit` to a bootstrap GameObject in the first scene to verify
the Unity package initializes at app start.

For repeatable real-backend validation, use the runnable project in
`../../Examples~/UnityPlayerE2E` and its `scripts/run_e2e.sh` runner.
